const Discord = module.require("discord.js");
module.exports.run = async (bot, message, args) => {
 //Тут пишите код команды
};

module.exports.help = {
  name: "name" //название команды по которому она будет вызываться
};
